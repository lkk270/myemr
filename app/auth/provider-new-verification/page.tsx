import { NewVerificationForm } from "@/auth/components/auth/new-verification-form";

const NewVerificationPage = () => {
  return <NewVerificationForm userType="PROVIDER" />;
};

export default NewVerificationPage;
